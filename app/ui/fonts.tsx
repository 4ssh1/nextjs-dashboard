
import { Inter, Lusitana } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  // you could add more options like weight, style etc as needed
});

export const lusitana = Lusitana({
  weight: ['400', '700'],     
  subsets: ['latin'],
});
